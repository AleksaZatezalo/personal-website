# DC381 Server Environment Configuration

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/dc381

# JWT Configuration
JWT_SECRET=your-very-secure-jwt-secret-key-change-this-in-production

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=uploads

# CORS Configuration (for development)
CORS_ORIGIN=http://localhost:3000

# Optional: MongoDB Atlas Configuration (comment out MONGODB_URI above if using Atlas)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dc381?retryWrites=true&w=majority

# Optional: Production JWT Secret (use a long, random string in production)
# JWT_SECRET=your-production-jwt-secret-key-minimum-32-characters-long